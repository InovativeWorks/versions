# versions
Version info for InovativeWorks products

⚠️ `proxypilot.json` / `smartpick.json` / `typeflow.json` は 2026-02-26 作成のプレースホルダ値（`version: "1.0.0"`）のまま実質的な更新履歴なし（smartpick.json のみ 2026-04-27 に 2 回の版数往復コミットがあるが、正味では作成時の値に戻っただけで実リリースに紐づく更新ではない）＝**ファイルの存在は出荷の証拠にならない**。実際のリリースに紐づく更新履歴があるのは `motionspice.json` / `rendersmith.json` のみ（＝実際に出荷済みの CEP 製品）。2026-07-13 ポートフォリオ再最適化裁定（`docs/strategy/portfolio_reoptimization_2026-07-12.md` 付録#8）による注記。
